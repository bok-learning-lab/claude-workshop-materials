title: Claude Workshop Materials
description: Bok Center AI faculty workshop — encyclopedia, activities, and project gallery.
baseurl: "/claude-workshop-materials"
url: "https://madxwoods.github.io"

layouts_dir: web/_layouts
data_dir: web/_data

markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

permalink: pretty

defaults:
  - scope:
      path: "encyclopedia"
    values:
      layout: default
      section: encyclopedia
  - scope:
      path: "encyclopedia/get-started"
    values:
      layout: default
      section: get-started
  - scope:
      path: "activities"
    values:
      layout: default
      section: activities
  - scope:
      path: "gallery"
    values:
      layout: default
      section: activities

exclude:
  - ".claude"
  - "node_modules"
  - Gemfile
  - Gemfile.lock
  - "*.sh"
